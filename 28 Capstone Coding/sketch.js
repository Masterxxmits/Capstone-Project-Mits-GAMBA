// GAMBA Casino
// Mitsaal Idris
// TBD

let logo; 

// function preload(){
//   logo = loadImage('assets/LOGO.png');
  
// }

function setup() {
  createCanvas(windowWidth, windowHeight);
  stroke(255);
  noFill();
  // image(logo, 0, 0);
  // logo.hide();
}

function draw() {
  // let ctx = (a, canvas, context);
  // ctx.canvas.width  = window.innerWidth;
  // ctx.canvas.height = window.innerHeight;
  
}


